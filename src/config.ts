// I don't care about the API key, so I'm just going to put it here.
// TODO: Maybe make it private and make a user input in widget prop?
export const apiKey = '04a04af9f9a7464cc8a738e1f0dbac8e'

/**
 * The key used to store the locations in local storage.
 */
export const locationsStorageKey = 'weather-widget-locations'
